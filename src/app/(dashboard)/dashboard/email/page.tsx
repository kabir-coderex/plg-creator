import { Mail } from "lucide-react"

import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function EmailPage() {
  return (
    <PagePlaceholder
      title="Email"
      description="Write and schedule campaigns with AI."
      icon={Mail}
    />
  )
}
