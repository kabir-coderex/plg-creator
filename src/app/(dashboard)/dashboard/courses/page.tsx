import { GraduationCap } from "lucide-react"

import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function CoursesPage() {
  return (
    <PagePlaceholder
      title="Courses"
      description="Build lessons, quizzes, and drip schedules."
      icon={GraduationCap}
    />
  )
}
