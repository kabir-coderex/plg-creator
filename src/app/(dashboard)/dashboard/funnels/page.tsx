import { Filter } from "lucide-react"

import { PagePlaceholder } from "@/components/layout/page-placeholder"

export default function FunnelsPage() {
  return (
    <PagePlaceholder
      title="Funnels"
      description="Design landing, checkout, and upsell flows."
      icon={Filter}
    />
  )
}
